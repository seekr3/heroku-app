{
  "name": "node-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "next": "^9.2.2",
    "postcss-simple-vars": "^5.0.2",
    "precss": "^4.0.0",
    "react": "^16.13.0",
    "react-dom": "^16.13.0",
    "styled-jsx": "^3.2.4",
    "styled-jsx-plugin-postcss": "^2.0.1",
    "tailwindcss": "^1.2.0"
  },
  "scripts": {
    "start": "NODE_ENV=production next build && NODE_ENV=production node index",
    "dev": "node index",
    "build": "next build",
    "export": "next build && next export",
    "commit": "git pull && git add . && git commit -am auto && git push",
    "clean": "rm -r .next && rm -r out"
  }
}
